{% extends 'base.html.twig' %}

{% block title %}Edit Language{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-6">
        <h1 class="text-2xl font-bold mb-4">Edit Language</h1>

        {{ include('language/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-4">
            <a href="{{ path('app_language_index') }}" class="text-blue-500 hover:underline">back to list</a>
        </div>

        {{ include('language/_delete_form.html.twig') }}
    </div>
{% endblock %}
